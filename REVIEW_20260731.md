# FH Protocol Compare — 设计回顾与改进清单

> 日期：2026-07-31
> 方法：对照 DESIGN.md（v1.0）+ CHANGELOG + 实际源码（src/）+ 一次真实比对产物（versions/20260731_...）
> 结论：端到端 pipeline 已跑通，测试 129/129 通过，会话清理已补。对照 DESIGN 仍有功能遗漏与设计/实现不一致。

---

## 一、功能性遗漏（与 DESIGN 明确不符）—— 高优先级

### 1. 报告溯源信息缺失「页码 / 段落号 / 文档名」（DESIGN §4）
- DESIGN 要求位置字段：`Base：{文档名}，第 {章}.{节} 节，P{页码}，段 {段落号}`。
- 实际 reporter.py 仅输出 `第 {base_section_number} 节`，**无页码、无段落号、无文档名**。
- 数据已具备：parser_pdf 产出 `<!-- page=N -->`，aligner.extract_sections 已提取 `page_hint`，但 reporter 未透传。
- 影响：违背"带原文溯源"核心承诺，溯源能力大幅削弱。

### 2. 语义覆盖度检查缺失（DESIGN §5.3）
- DESIGN 明确要求："若某核心功能在两文档中均出现但未被对齐，人工告警"。
- aligner.py 只做匹配 + 标记独有，**未实现覆盖度检查/告警**。

### 3. Base 独有章节被无脑标 `feature-removed`（质量隐患）
- analyzer.analyze_diff_item 对无 compare_id 的条目**直接硬编码** `feature-removed`，不调 LLM。
- 但 DESIGN §5.3 明确警告：O-RAN 与 ASTRI 是不同标准组织，结构本就不同，Base 独有 ≠ 功能删除。
- 实测报告：152 条 feature-removed 占 196 总条的 78%，严重虚高，结论不可信。
- 建议：Base 独有也应走 LLM 判断（是"标准范围差异/未覆盖"还是"真删除"）。

---

## 二、依赖与配置问题 —— 中优先级

### 4. requirements.txt 不准
- **缺 `httpx`**：analyzer.py 异步并发（`analyze_diff_batch_async`）实际依赖，漏装必崩。
- **含未使用重依赖**：`sentence-transformers` / `chromadb` / `onnxruntime`（aligner 实际用 scikit-learn TF-IDF，未用 embedding/chroma）。
- 建议：补 httpx、移除未使用项、瘦身安装。

### 5. DESIGN §6.1 端点端口过时
- DESIGN/CHANGELOG 写 19000 proxy → 28789 gateway；实际是 **61791 Gateway + Bearer token + model "openclaw"**（llm_client.py 已修正，文档未同步）。

### 6. DESIGN §6.2 按任务选型模型未实现
- DESIGN 说对齐用便宜模型、分析用强模型可配置；实际 aligner 不用 LLM、analyzer 硬编码 `model="openclaw"`，settings 无对应字段。

---

## 三、工程卫生 / 文档同步 —— 低优先级

### 7. 仓库根目录散落调试文件
- `test_extract.py` / `test_extract2.py` / `test_llm.py` / `verify.ps1` / `verify_session_cleanup.py` + `tests/debug_differ.py` 均为临时调试产物（test_extract2 本应删除），应清理或移入 tests/。

### 8. DESIGN §5.1 `specs/` 目录未使用
- 结构化提取产物实际存进 `versions/`，`specs/` 成死目录（磁盘上不存在）。

### 9. DESIGN §7.1 "Compare 多个并行" 实际串行
- main.run_batch 顺序执行（可保留以避限流，但文档应注明）。

### 10. DESIGN §十二 开放问题"影响等级标准"
- 实现中 prompt 已定义高/中/低标准，可在 DESIGN 关闭该 open question。

### 11. 产物命名差异
- DESIGN 写 `diff_raw.md`，实际 `diff_raw.json`（另加 analyzed.json/stats.json）。文档同步即可。

---

## 四、输出质量观察

### 12. TOC 锚点碰撞
- 多个 diff 共用节标题（如 "Categories for O-DU with fixed timing advance"）生成重复 TOC 项与空锚点 `#19.-`，跳转失效。

### 13. feature-removed 占比畸高（152/196）
- 见 §3，建议修正判定逻辑后重看分布。

### 14. emoji 显示 `??`（已核实非 bug）
- report.md 实际为合法 UTF-8（含 📊/🟥 等 4 处 4-byte 序列、无 BOM）；`??` 仅为 PowerShell 控制台渲染问题，文件本身正常。

---

## 五、建议优先级

| 优先级 | 条目 |
|--------|------|
| P0 必改 | #1 报告页码溯源、#3 Base独有误标、#4 httpx 依赖 |
| P1 建议改 | #2 覆盖度检查、#5/#6 文档端口与模型同步、#12 TOC 锚点 |
| P2 整理 | #7 清理调试文件、#8/#9/#10/#11 文档同步 |

---

## 六、总体评价
架构合理、pipeline 完整、测试充分、会话清理已闭环。最大风险是**报告结论可信度**（§3 虚高的 feature-removed 与 §1 缺失溯源），其次是**依赖声明不准**（§4 httpx 缺失会导致干净环境安装后运行崩溃）。建议优先解决 P0。
