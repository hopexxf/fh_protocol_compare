# FH Protocol Compare — 依赖清单
# 安装：py -3 -m pip install -r requirements.txt

# 文档解析
pdfplumber>=0.11.0
pdfminer.six>=20231228
pypdfium2>=4.0
python-docx>=1.0.0

# 表格提取（基于 Java）
camelot-py>=2.0.0

# 相似度计算（章节对齐，TF-IDF + 余弦）
scikit-learn>=1.3.0

# 配置
PyYAML>=6.0

# HTTP 请求（LLM 调用）
requests>=2.31.0      # 同步调用（llm_client.py）
httpx>=0.24.0        # 异步并发批量（analyzer.py）

# 文本 diff（从 vendor/ 加载，无需 pip 安装）
# diff_match_patch — 从 vendor/diff_match_patch.py 加载，已纳入仓库
